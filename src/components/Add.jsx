import styled from 'styled-components';

const Add = styled.button`
  grid-area: add;
  margin: 20px;
  background: none;
  border: 1px dashed #bbb;
  cursor: pointer;
  font-size: 30px;
`;

export default Add;
