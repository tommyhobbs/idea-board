import styled from 'styled-components';

const Bin = styled.button`
  grid-area: bin;
  padding: 20px;
  background: none;
  border: none;
  cursor: pointer;
  span {
    font-size: 30px;
  }
`;

export default Bin;
